@{
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="keywords" content="hmcoding, hm coding, HMCoding, HM Coding, web ôn luyện thi lập trình, lập trình">
    <meta name="description" content="HM Coding - Nền tảng học lập trình trực tuyến">
    <meta name="author" content="Hậu Nguyễn và Thảo Lê">

    <meta name="copyright" content="Hậu Nguyễn và Thảo Lê">

    <link rel="stylesheet" href="~/lib/bootstrap/theme/bootstrap.css">
    <link rel="stylesheet" href="~/css/bootstrap.extend.css">
    <link rel="stylesheet" href="~/lib/sweetalert2/themes/minimal/minimal.min.css">
    <link rel="stylesheet" href="~/css/button-extend.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true"/>
    <link rel="stylesheet" href="~/css/rank.css" asp-append-version="true" />
    <link href="~/lib/DataTables/datatables.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Nunito">
    <link href="https://cdn.datatables.net/v/bs5/jq-3.7.0/dt-2.2.2/datatables.min.css" rel="stylesheet" integrity="sha384-WMi+Ec+QE8hxW/3qKvuefShIddYjwMalSgy0MR4FZnl285C4HGYfISceaagw0Am3" crossorigin="anonymous">
    <link href="~/lib/fontawesome/css/all.min.css" rel="stylesheet">
    <link rel="icon" href="~/assets/icons/logo.png" />

    @RenderSection("Links", required: false)

    @if (ViewData["Title"] != null)
    {
        <title>@ViewData["Title"] | HMCoding</title>
    }
    else
    {
        <title>HMCoding</title>
    }

</head>

<body>
    <header>
        <nav class="navbar navbar-expand-lg" style="user-select:none;">
            <div class="container-fluid">
                <a class="navbar-brand" href="/">
                    <h3 class="text-center text-info m-0">HMCoding</h3>
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse" id="navbarColor01">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link @(currentController == "Home" ? "active" : "")" asp-action="Index" asp-controller="Home">
                                Trang chủ
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(currentController == "CodePad" ? "active" : "")" asp-action="Index" asp-controller="Codepad">Codepad</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(currentController == "Exercise" ? "active" : "")" asp-action="Index" asp-controller="Exercise">Bài tập</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(currentController == "Marking" ? "active" : "")" asp-action="Index" asp-controller="Marking">Bảng chấm bài</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(currentController == "Rank" && currentAction == "RankingList" ? "active" : "")" asp-action="RankingList" asp-controller="Rank">Xếp hạng</a>
                        </li>
                        @* <li class="nav-item">
                            <a class="nav-link @(currentController == "Meeting" ? "active" : "")" asp-action="Index" asp-controller="Meeting">MEETING</a>
                        </li> *@
                    </ul>
                    @if (User.Identity?.IsAuthenticated ?? false)
                    {
                        <div class="nav-item dropdown p-3 notification-dropdown">
                            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" id="notificationDropdown">
                                <i class="fa-solid fa-bell notification-bell"></i>
                                <span class="notification-badge" id="unseenCount" style="display: none;">0</span>
                            </a>
                            <div class="dropdown-menu dropdown-menu-end notification-menu">
                                <h1 class="dropdown-header text-center text-success">Thông báo</h1>
                                <div id="notificationList"></div>
                            </div>
                        </div>
                        <div class="nav-item dropdown p-3">
                            <a class="nav-link dropdown-toggle text-success @(currentController == "UserInfo" ? "active" : "")" data-bs-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                                <img id="userAvatar"
                                     src="~/assets/images/avartardefault.jpg"
                                     class="rounded-circle"
                                     alt="User Avatar"
                                     loading="lazy" />
                                @User.FindFirst(ClaimTypes.Name)?.Value
                            </a>
                            <div class="dropdown-menu dropdown-menu-end">
                                <a class="dropdown-item" asp-route-id="" asp-action="Details" asp-controller="UserInfo">Thông tin tài khoản</a>
                                @if (User.IsInRole("admin") || User.IsInRole("teacher"))
                                {
                                    <a class="dropdown-item" asp-action="Index" asp-controller="Home" asp-area="Admin">Tới trang quản lý</a>
                                }
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item text-danger" asp-action="Logout" asp-controller="User">Đăng xuất</a>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="nav-item auth-links">
                            <a class="nav-link" asp-action="Login" asp-controller="User">Đăng nhập</a>
                        </div>
                        <div class="nav-item auth-links">
                            <a class="nav-link" asp-action="Signup" asp-controller="User">Đăng ký</a>
                        </div>
                    }
                </div>
            </div>
        </nav>
    </header>

    <main id="main">
        @RenderBody()
    </main>


    <!-- Nội dung body khác -->

    <div id="footer">
        <div id="" class="pb-3 pt-3 bg-dark">
            <div class="online-users" style="display:flex; align-items:center; justify-content:center; gap:0.7rem; color: gainsboro;font-weight:500;">
                <b>Người dùng đang online: <span id="onlineUsersList">Đang tải...</span></b>
            </div>
        </div>
        <img src="~/assets/images/ToanhaA-01.png" alt="">
        <div class="container">
            <p class="address1" style="display:flex;align-items:center;justify-content:center; gap:0.7rem;">Tác giả: Hậu Nguyễn và Thảo Lê</p>
            <p class="address2" style="display:flex;align-items:center;justify-content:center; gap:0.7rem;">Khoa Công nghệ Thông tin - Đại học Sư phạm TP. Hồ Chí Minh</p>
            <div class="phonenumber mt-0" style="display:flex;align-items:center;justify-content:center; gap:0.7rem;">
                <p>Liên hệ: 0944746748</p>
                <p>Email: haunv.cntt@gmail.com</p>
            </div>

        </div>
    </div>

    <!-- Notification Modal -->
    <div class="modal fade" id="notificationModal" tabindex="-1" aria-labelledby="notificationModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="notificationModalLabel"></h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p id="notificationMessage"></p>
                    <p class="notification-meta" id="notificationMeta"></p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-primary" data-bs-dismiss="modal">Đóng</button>
                </div>
            </div>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.min.js"></script>
    <script src="~/lib/sweetalert2/dist/sweetalert2.min.js"></script>
    <script src="~/lib/DataTables/datatables.min.js"></script>
    <script src="~/js/sweetalert2-extend.js"></script>
    <script src="~/js/site.js"></script>
    <script src="https://kit.fontawesome.com/0f2d543076.js" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/8.0.0/signalr.min.js"></script>
    <script>
        function getClassNameByAuth(auth){
            switch (auth) {
                case "admin":
                    return "badge bg-danger";
                case "teacher":
                    return "badge bg-success";
                case "hsgstudent":
                    return "badge bg-info";
                default:
                    return "badge bg-secondary";
            }
        }

        function getTitleByAuth(auth){
            switch (auth) {
                case "admin":
                    return "Quản trị viên";
                case "teacher":
                    return "Giáo viên";
                case "hsgstudent":
                    return "Học sinh giỏi";
                default:
                    return "Người dùng";
            }
        }

        const currentUserName = `@(User.FindFirst(ClaimTypes.Name)?.Value)`;
        var listUserOnline = [];
        
        const connection = new signalR.HubConnectionBuilder()
            .withUrl("/onlineUsersHub")
            .withAutomaticReconnect()
            .build();

        // Nhận danh sách người dùng online
        connection.on("ReceiveOnlineUsers", function (users) {
            const onlineUsersList = document.getElementById("onlineUsersList");
            if (users && users.length > 0) {
                const userList = users.map(user => `
                    <a
                        style="text-decoration:none;"
                        class="${getClassNameByAuth(user.auth)}"
                        title="${getTitleByAuth(user.auth)}"
                        href="/userinfo/details/${user.userId}">
                        ${user.username}
                    </a>`);
                onlineUsersList.innerHTML = userList.join(" ");
                listUserOnline = users.map(user => user.username);
            } else {
                onlineUsersList.textContent = "Không có";
                listUserOnline = [];
            }
        });

        // Ping định kỳ (và khi tương tác)
        let pingInterval;
        let lastPingTime = 0;
        const MIN_PING_INTERVAL = 30000; // 30 giây

        function safePing() {
            const now = Date.now();
            
            // Chỉ gửi Ping nếu đã đủ thời gian tối thiểu hoặc người dùng đã đăng nhập không có trong listUserOnline
            if (now - lastPingTime >= MIN_PING_INTERVAL || (currentUserName && !listUserOnline.includes(currentUserName))) {
                lastPingTime = now;
                connection.invoke("Ping").catch(err => console.error("Ping lỗi:", err.toString()));
            }
        }

        // Gọi khi kết nối thành công
        connection.start().then(function () {
            connection.invoke("GetOnlineUsers").catch(function (err) {
                console.error(err.toString());
                document.getElementById("onlineUsersList").textContent = "Lỗi khi tải danh sách người dùng";
            });

            // Ping định kỳ mỗi 60 giây
            pingInterval = setInterval(safePing, 60000);

            // Gọi Ping khi tab được mở lại
            document.addEventListener("visibilitychange", function () {
                if (document.visibilityState === "visible") {
                    safePing();
                }
            });

            // Gọi Ping khi người dùng tương tác
            ['scroll', 'mousemove', 'keydown', 'click'].forEach(event => {
                window.addEventListener(event, () => {
                    safePing();
                }, { passive: true });
            });
        }).catch(function (err) {
            console.error(err.toString());
            document.getElementById("onlineUsersList").textContent = "Lỗi kết nối SignalR";
        });

        // Reconnect
        connection.onreconnected(function () {
            console.log("Reconnected to SignalR Hub");

            // Gọi lại GetOnlineUsers
            connection.invoke("GetOnlineUsers").catch(function (err) {
                console.error(err.toString());
                document.getElementById("onlineUsersList").textContent = "Lỗi khi tải danh sách người dùng";
            });

            // Reset Ping sau reconnect
            if (pingInterval) clearInterval(pingInterval);
            pingInterval = setInterval(safePing, 60000);
        });
    </script>


    @if (User.Identity?.IsAuthenticated ?? false)
    {
        <script src="~/js/notification.js"></script>
        <script>
            
        </script>
    }


    @if (User.Identity?.IsAuthenticated ?? false)
    {
        <text>
            <script>
                $(document).ready(function () {
                    var avatar = localStorage.getItem("UserAvatar");
                    if(avatar) {
                        $("#userAvatar").attr("src", "data:image/jpeg;base64," + avatar);
                    } else {
                        $.ajax({
                                    url: '/UserInfo/GetAvatar',
                                    type: 'GET',
                                    success: function (response) {
                                        if (response.status) {
                                            if(response.avatar){
                                                localStorage.setItem("UserAvatar", response.avatar);
                                                $("#userAvatar").attr("src", "data:image/jpeg;base64," + response.avatar);
                                            } else {
                                                localStorage.removeItem("UserAvatar");
                                                $("#userAvatar").attr("src", "/assets/images/avartardefault.jpg");
                                            }
                                        }else{
                                            localStorage.removeItem("UserAvatar");
                                            $("#userAvatar").attr("src", "/assets/images/avartardefault.jpg");
                                        }
                                    },
                                    error: function () {
                                        console.log("Failed to load avatar.");
                                        $("#userAvatar").attr("src", "/assets/images/avartardefault.jpg");
                                    }
                                });

                    }

                });
            </script>
        </text>
    }

    

    @RenderSection("Scripts", required: false)
</body>
</html>